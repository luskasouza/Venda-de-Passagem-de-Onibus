create database bancoPOO;

use bancoPOO;

create table cliente(
idCliente int not null,
nome varchar(64) not null,
cpf varchar(16) not null,
rg varchar(10) not null,
primary key(idCliente)
);

create table cidade(
idCidade int not null,
cidadeAtual varchar(64),
cidadeDestino varchar(64),
primary key(idCidade)
);

create table usuario(
id_Usuario int not null,
email varchar(35) not null,
password varchar(15) not null,
primary key(id_Usuario)
);




create table passagens(
idPassagens int not null,
quantidadeDePassagens int,
precoPassagens double,
dataDacompra date,
horaDacompra time,
primary key(idPassagens)
);

--OBS: Use o heidisql e colóquio auto incremento banco de dados
